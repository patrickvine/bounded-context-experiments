== README

Bounded Context experiments

Assume a User model with validations.
Assume a need for an invitable user model which validates that there is an email.
How could one implement it?

View spec/models/user_spec.rb for examples.
